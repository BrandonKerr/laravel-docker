
  mailpit:
    image: axllent/mailpit:latest
    container_name: __PROJECT_NAME__-mailpit
    restart: unless-stopped
    ports:
      - "8025:8025"
    networks:
      - __PROJECT_NAME__
    healthcheck:
      test: ["/mailpit", "readyz"]
      interval: 30s
      timeout: 10s
      retries: 3
